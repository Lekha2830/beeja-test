import { styled } from "styled-components";

export const InputErrorMessage = styled.span`
  color: ${props=>props.theme.colors.stateColors.error || '#E03137'};
font-family: ${props=>props.theme.theme || 'Nunito'};;
font-size: 12px;
font-style: normal;
font-weight: 400;
line-height: 160%;

display: flex;
align-items: center;
gap: 3px;
`